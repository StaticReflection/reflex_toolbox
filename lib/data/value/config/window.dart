import 'package:flutter/material.dart';

class AppWindowConfig {
  static const Size minimumSize = Size(1280, 720);
  static const Size initialSize = minimumSize;
  static const Alignment initialAlignment = Alignment.center;
}
